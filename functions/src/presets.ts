import { Presets } from './@types'

const presets: Presets = {
	default: {},
	map_preview: {
		width: 160,
		height: 104,
		format: 'webp',
	},
	profile_roll: {
		width: 240,
		height: 160,
		format: 'webp',
	},
}

export default presets
