{
  "engines": {
    "node": "8"
  },
  "name": "functions",
  "scripts": {
    "lint": "tslint --project tsconfig.json",
    "build": "tsc",
    "serve": "npm run build && firebase serve --only functions --port 5050",
    "shell": "npm run build && firebase functions:shell",
    "start": "npm run shell",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@google-cloud/storage": "^2.5.0",
    "@types/sharp": "^0.22.1",
    "cors": "^2.8.4",
    "firebase-admin": "^7.3.0",
    "firebase-functions": "^2.3.1",
    "sharp": "^0.22.1"
  },
  "devDependencies": {
    "firebase-functions-test": "^0.1.6",
    "tslint": "^5.16.0",
    "typescript": "^3.4.5"
  },
  "private": true
}
